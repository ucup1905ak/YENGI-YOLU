#include <stdio.h>
#include "header.h"
int main(){
    bool authentication = false;
    int percobaan = 3;
    while(!authentication){
        system("cls");
        // loginDisplay(&percobaan, &authentication);
    }
    while(1) {

        // printf("\nCaptha: ");generateChapta();
        delay(1000);
    }
    return 0;
}